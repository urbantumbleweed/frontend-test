describe('<App />', () => {
  describe('initialize', () => {
    test.todo('the initial window.location is `/restaurants`')
  })
  describe('state', () => {
    test.todo('load `categories` from cache before refetching')
  })
  describe('styles', () => {
    test.todo('`background-color` matches spec')
    test.todo('`margin` is zero')
  })
  describe('render', () => {
    test.todo('<ListLayout /> when route is `/restaurants`')
    test.todo('<DetailsLayout /> when route is `/restaurants/:id`')
  })
})