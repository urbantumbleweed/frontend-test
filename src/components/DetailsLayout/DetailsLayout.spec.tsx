describe('<DetailsLayout />', () => {
  describe('initialize', () => {
    test.todo('the initial window.location is `/restaurants/<id>`')
  })
  describe('state', () => {
    test.todo('has a `pageTitle` that is a Restaurant name')
  })
  describe('handlers', () => {
    test.todo('`componentDidMount` calls `fetchReviews`')
    test.todo('`componentDidMount` calls `fetchBusinessDetails`')
  })
  describe('styles', () => {
    test.todo('`margin-left` matches spec spec value of `64`')
    test.todo('`margin-top` matches spec spec value of `36`')
    test.todo('`margin-right` matches spec spec value of `64`')
  })
  describe('render', () => {
    test.todo('<PageHeader />')
    test.todo('<Rating /> with a prop `variant:large`')
    test.todo('<LocationView />')
    test.todo('<Gallery />')
    test.todo('<ReviewSection />')
  })
})